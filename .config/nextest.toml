[profile.default]
fail-fast = false
slow-timeout = { period = "60s", terminate-after = 2 }

[profile.ci]
fail-fast = false
retries = 0
status-level = "pass"
failure-output = "immediate-final"
slow-timeout = { period = "60s", terminate-after = 2 }
