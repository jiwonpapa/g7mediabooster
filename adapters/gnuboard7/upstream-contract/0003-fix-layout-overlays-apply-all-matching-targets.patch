diff --git a/app/Services/LayoutExtensionService.php b/app/Services/LayoutExtensionService.php
index cdc338c1..71dbfab7 100644
--- a/app/Services/LayoutExtensionService.php
+++ b/app/Services/LayoutExtensionService.php
@@ -883,23 +883,34 @@ private function applyOverlays(array $layout, int $templateId, array $targetName
      */
     private function injectAtTarget(array &$components, string $targetId, string $position, array $newComponents): bool
     {
-        foreach ($components as $index => &$component) {
-            // 타겟 ID 발견
-            if (($component['id'] ?? '') === $targetId) {
-                $this->injectComponents($components, $component, $index, $position, $newComponents);
-
-                return true;
-            }
-
-            // 자식에서 재귀 탐색
-            if (isset($component['children']) && is_array($component['children'])) {
+        $injected = false;
+
+        // prepend/append는 형제 배열 길이와 인덱스를 바꿉니다. 원본 인덱스를 뒤에서부터
+        // 처리해야 새로 삽입한 컴포넌트를 다시 타겟으로 순회하는 무한 반복을 막을 수 있습니다.
+        for ($index = count($components) - 1; $index >= 0; $index--) {
+            $component = &$components[$index];
+            $matchesTarget = ($component['id'] ?? '') === $targetId;
+
+            // replace가 아닌 경우 원래 자식의 같은 타겟도 먼저 처리합니다. 부모에 새로
+            // 추가되는 children은 이번 overlay에서 다시 처리하지 않습니다.
+            if ((! $matchesTarget || $position !== 'replace')
+                && isset($component['children'])
+                && is_array($component['children'])
+            ) {
                 if ($this->injectAtTarget($component['children'], $targetId, $position, $newComponents)) {
-                    return true;
+                    $injected = true;
                 }
             }
+
+            if ($matchesTarget) {
+                $this->injectComponents($components, $component, $index, $position, $newComponents);
+                $injected = true;
+            }
+
+            unset($component);
         }
 
-        return false;
+        return $injected;
     }
 
     /**
@@ -970,6 +981,8 @@ private function injectComponents(
      */
     private function injectPropsAtTarget(array &$components, string $targetId, array $propsToInject, ?array $injectMeta = null): bool
     {
+        $injected = false;
+
         foreach ($components as &$component) {
             if (($component['id'] ?? '') === $targetId) {
                 $this->injectProps($component, $propsToInject);
@@ -982,17 +995,18 @@ private function injectPropsAtTarget(array &$components, string $targetId, array
                     $component['__injectedProps'][] = $injectMeta;
                 }
 
-                return true;
+                $injected = true;
             }
 
+            // props 주입은 형제 배열을 변경하지 않으므로 타겟 자신의 자식도 계속 순회합니다.
             if (isset($component['children']) && is_array($component['children'])) {
                 if ($this->injectPropsAtTarget($component['children'], $targetId, $propsToInject, $injectMeta)) {
-                    return true;
+                    $injected = true;
                 }
             }
         }
 
-        return false;
+        return $injected;
     }
 
     /**
diff --git a/tests/Unit/Services/LayoutExtensionServiceTest.php b/tests/Unit/Services/LayoutExtensionServiceTest.php
index d0ebe502..c7b4d191 100644
--- a/tests/Unit/Services/LayoutExtensionServiceTest.php
+++ b/tests/Unit/Services/LayoutExtensionServiceTest.php
@@ -612,6 +612,68 @@ public function test_applies_overlay_to_module_prefixed_requested_layout(): void
         $this->assertSame('admin_board_native_file_uploader', $result['components'][1]['id']);
     }
 
+    /**
+     * 조건부 폼이 같은 partial을 여러 번 재사용하면 렌더 시점에는 한 분기만 보이지만,
+     * 병합된 JSON 트리에는 같은 안정 ID가 여러 개 존재한다. 모든 일치 노드에 컴포넌트와
+     * props를 적용해야 basic/gallery/card 어느 분기에서도 확장이 빠지지 않는다.
+     */
+    public function test_applies_component_and_props_injections_to_every_matching_target(): void
+    {
+        LayoutExtension::create([
+            'template_id' => $this->template->id,
+            'extension_type' => LayoutExtensionType::Overlay,
+            'target_name' => 'board/form',
+            'source_type' => LayoutSourceType::Module,
+            'source_identifier' => 'sirsoft-marketing',
+            'content' => [
+                'target_layout' => 'board/form',
+                'injections' => [
+                    [
+                        'target_id' => 'native_uploader',
+                        'position' => 'prepend',
+                        'components' => [[
+                            'id' => 'external_uploader',
+                            'type' => 'basic',
+                            'name' => 'Div',
+                        ]],
+                    ],
+                    [
+                        'target_id' => 'submit_button',
+                        'position' => 'inject_props',
+                        'props' => ['disabled' => true],
+                    ],
+                ],
+            ],
+            'priority' => 100,
+            'is_active' => true,
+        ]);
+
+        $branch = static fn (): array => [
+            'type' => 'basic',
+            'name' => 'Div',
+            'children' => [
+                ['id' => 'native_uploader', 'type' => 'basic', 'name' => 'Div'],
+                [
+                    'id' => 'submit_button',
+                    'type' => 'basic',
+                    'name' => 'Button',
+                    'props' => ['disabled' => false],
+                ],
+            ],
+        ];
+
+        $result = $this->service->applyExtensions([
+            'layout_name' => 'board/form',
+            'components' => [$branch(), $branch(), $branch()],
+        ], $this->template->id);
+
+        foreach ($result['components'] as $renderBranch) {
+            $this->assertSame('external_uploader', $renderBranch['children'][0]['id']);
+            $this->assertSame('native_uploader', $renderBranch['children'][1]['id']);
+            $this->assertTrue($renderBranch['children'][2]['props']['disabled']);
+        }
+    }
+
     /**
      * Overlay 확장이 호스트 레이아웃 init_actions 뒤에 초기화 액션을 병합한다.
      *
